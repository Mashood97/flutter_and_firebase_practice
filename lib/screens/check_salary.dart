import 'package:flutter/material.dart';

class CheckSalary extends StatelessWidget {
  static const routeArgs ='/checksalar';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
            appBar: AppBar(title: Text('Check Salary'),),

    );
  }
}